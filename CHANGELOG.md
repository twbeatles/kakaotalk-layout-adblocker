# Changelog

## 11.1.5 - 2026-09-24

`PROJECT_AUDIT.md`(2026-09-24) 감사 지적 사항을 반영했습니다. 광고 판정 알고리즘과 골든 fixture 기대값은 바꾸지 않았습니다. 명세·작업 목록은 `specs/002-audit-2026-09-24-remediation/`에 있습니다.

### 수정
- **BOM이 붙은 설정 파일 초기화 (ISSUE-001)**: PowerShell 5.1의 `Set-Content -Encoding utf8`나 메모장의 "UTF-8(BOM)"으로 저장한 정상 JSON이 "손상"으로 판정되어 기본값으로 덮어써지던 문제를 고쳤습니다.
- **카카오톡 응답 없음 상태에서 종료가 멈춤 (ISSUE-003)**: 종료 시 창 복원 호출이 응답 없는 카카오톡에 막히면, 트레이 아이콘은 사라졌는데 프로세스가 남아 재실행이 "이미 실행 중"으로 거부됐습니다. 이제 응답 없는 창은 건드리지 않고, 종료 대기는 3초로 제한합니다.
- **거짓 "복원 실패" (ISSUE-004)**: 부모 창이 숨겨진 상태(카카오톡을 트레이로 닫은 상태 등)에서 자식 창을 정상 복원해도 실패로 집계되던 문제를 고쳤습니다. 판정 기준을 창 자신의 표시 상태로 바꾸고, 복원은 상위 창부터 합니다.
- **오래된 오류 표시가 남음 (ISSUE-005)**: 복원이 회복되면 트레이의 복원 오류 문구도 자동으로 사라집니다.

### 성능
- **유휴 CPU 약 77% 감소**: 카카오톡 실행 중 유휴 상태에서 60초 CPU 시간이 406ms에서 94ms로 줄었습니다(코어 1개 기준 0.677% → 0.156%, ARM64 에뮬레이션 환경 측정). 측정 조건과 README/BENCHMARK의 근거 없던 수치(0.01%, 4~7MB, 1.8MB, 5ms) 정정 내용은 `BENCHMARK.md`에 있습니다.
- **카카오톡 미실행 시 프로세스 스캔 (ISSUE-002)**: 카카오톡이 꺼져 있으면 전체 프로세스 목록(이 측정 환경에서 1회 약 6.5ms)을 200ms마다 다시 읽었습니다. 이제 5초 뒤 1초, 30초 뒤 2초 간격으로 늘어납니다. 실행 중일 때의 전체 재동기화도 5초에서 30초로 늘렸고, 생존 확인은 보관한 프로세스 핸들로 합니다.
- **유휴 재확인 주기 백오프**: 카카오톡 창 이벤트가 10초 넘게 없으면 재확인 주기를 200ms에서 최대 1초(`idle_backoff_max_ms`)까지 늘립니다. 이벤트가 오면 즉시 원래 주기로 돌아옵니다.
- **창 트리 수집**: 노드마다 하위 창 전체를 다시 열거하던 방식을 top-level당 1회 열거로 바꿨습니다. 이전 방식과 결과가 같은지 fixture 전체와 실제 데스크톱(584개 창)에서 확인했습니다.
- 이미 숨긴 창의 스냅샷 재캡처, 노드마다 반복되던 광고 토큰 소문자 변환·자식 목록 복제를 없앴습니다. 이벤트 병합 대기 중에도 메시지를 처리해 한 번의 이벤트 묶음이 두 번의 재확인을 유발하지 않게 했습니다.
- 릴리스 빌드에 LTO·`codegen-units=1`·`strip`을 적용했습니다(EXE 4.51MB → 4.26MB).

### 변경
- 새 설정 `idle_backoff_max_ms`(기본 1000). `idle_poll_interval_ms` 이하로 두면 백오프를 끕니다.
- 시작 시 설정 자동 복구 같은 경고 1건을 트레이 상태에 표시합니다.
- 시작프로그램 Run 등록이 있는데 설정이 꺼져 있으면 설정과 트레이 체크 표시를 실제 등록 상태에 맞춥니다.
- 트레이 메뉴 토글은 실행 중에 직접 고친 JSON 값을 덮어쓰지 않습니다.
- 업데이트 교체에 실패하면 이전 버전을 다시 실행하고 임시 파일을 정리합니다.
- 엔진 처리 중 예기치 못한 오류(panic)가 나도 숨긴 창 정보를 잃지 않고 계속 동작하며, 트레이에 오류를 표시합니다.
- 설정 저장 시 디스크 동기화(`sync_all`) 후 교체합니다. 차단 OFF 동안에는 창 이벤트 훅을 해제합니다.

### 구조
- Rust 장문 파일 6개를 단일 책임 하위 모듈로 분할했습니다(`evaluate` 5개, `engine` 7개, `tray` 6개, `updater` 7개, `config` 4개 모듈 + `lib.rs`에서 `args`/`dialogs`/`observability`/`dump_cmd`/`startup_repair` 추출). 순수 코드 이동이며 광고 판정 알고리즘과 모든 공개 모듈 경로(`kakao_app::{Args, ...}`, `engine::{tick, ...}`, `tray::{...}` 등)는 그대로입니다. 기존 `.rs` 파일은 `pub use` 퍼사드로 유지됩니다.
- `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --workspace`를 모두 통과했습니다.

## 11.1.4 - 2026-09-12

`PROJECT_AUDIT.md`(2026-09-12) 감사에서 확인된 장기 상주 안정성 문제와 문서·설정 불일치를 수정한 릴리스입니다.

### 수정
- **로그 무한 증가 (ISSUE-001)**: 로그 회전이 프로세스 시작 시 1회만 수행되어, 시작프로그램으로 며칠~몇 주 상주하면 `layout_adblock.log`가 제한 없이 커졌습니다. 이제 실행 중에도 5MB 초과 시 `layout_adblock.log.1`로 회전합니다.
- **복원 재시도 폭주 (ISSUE-002)**: 복원에 계속 실패하는 창을 매 tick 재시도하며 경고 로그와 실패 카운터를 무한히 쌓던 문제를 고쳤습니다. 재시도는 지수 백오프(1→2→4→8 tick, 이후 약 60초)로 늘어나고 경고는 창당 1회만 남습니다. `복원 실패` 수치는 누적 시도 횟수가 아니라 **현재 복원에 실패한 창 수**를 의미합니다.
- **중복 실행 시 무반응 (ISSUE-005)**: 이미 실행 중일 때 EXE를 다시 실행하면 안내 없이 조용히 종료되던 문제를 고쳤습니다(`std::env::args()`에 실행 파일 경로가 포함되어 판정이 항상 뒤집혔습니다). 이제 "프로그램이 이미 실행 중입니다." 안내가 표시됩니다.

### 성능
- **탐지 루프 중복 작업 제거 (ISSUE-004)**: 매 tick마다 진단용 후보 목록 생성을 위해 전체 재귀 순회를 한 번 더 수행하고 상태 맵 전체를 복제한 뒤 그 결과를 버리고 있었습니다. 적용 전용 평가 경로(`evaluate_graph_for_apply`)를 분리했습니다. `--dump-tree`/`--dump-tree-series`/`--shadow` 출력은 그대로입니다.
- **WinEvent 훅 범위 축소 (ISSUE-006)**: 세션 전체 이벤트를 받던 훅을 카카오톡 PID로 한정했습니다. 다른 창을 드래그할 때 발생하는 대량의 `EVENT_OBJECT_LOCATIONCHANGE`가 이벤트 큐를 채워 카카오톡 이벤트를 밀어내던 경로가 사라졌습니다.

### 변경
- **설정 3종을 실제로 연결 (ISSUE-003)**: `poll_interval_ms`(활성 상태 재확인 주기)와 `cache_cleanup_interval_ms`(캐시 정리 주기)가 이전에는 파싱만 되고 동작에 반영되지 않았습니다. 이제 실제로 적용됩니다. `start_minimized`는 트레이 전용 런타임에서 의미가 없어 README에 "현재 미사용"으로 명시했습니다.
- **트레이 상태 표시 추가**: 메뉴 헤더와 트레이 아이콘 툴팁에 차단/공격 모드, 메인윈도우 수, 누적 숨김·닫힘·리사이즈, 복원 실패 수, 마지막 오류를 표시합니다. `복원 실패 초기화`는 실패가 있을 때만 활성화됩니다.
- **`--self-check` 보강**: README가 약속한 대로 `HKCU Run` 레지스트리 읽기/쓰기 접근, 시작프로그램 등록 명령 상태, 프로세스 열거를 실제로 점검합니다. `--strict-self-check`는 설정 자동 복구 같은 정보성 경고로는 더 이상 실패하지 않습니다(빌드 머신의 `%APPDATA%` 상태가 릴리스 빌드를 깨뜨리던 문제).
- **`closed_windows` 카운터 복구**: 증가 코드가 없어 항상 0이던 값이 실제 close 요청 수를 반영합니다. 트레이의 `누적 닫힘`은 `WM_CLOSE` 후 창 소멸이 확인된 건수만 셉니다.

### 안정성
- 팝업이 `WM_CLOSE`를 거부하거나 전달되지 않은 사실을 `DEBUG` 로그에 남깁니다(이전에는 결과를 버렸습니다). 동작 자체는 기존과 같이 hide/zero-size 대체 경로가 적용됩니다.
- 설정 파일의 임시 파일명에 PID를 포함해, mutex 밖에서 도는 `--self-check`와 트레이 앱이 동시에 쓸 때 서로를 덮어쓰지 않습니다.
- 업데이트 헬퍼가 교체 직전에 SHA-256을 재검증하고, 재실행 시 원래 실행 인자(`--startup-launch`/`--minimized`)를 이어받으며, 롤백 실패를 로그에 남깁니다.
- `HKCU Run` 값을 읽을 때 버퍼 크기를 먼저 조회해, 1023자를 넘는 명령이 "등록 없음"으로 오인되어 덮어써지지 않게 했습니다.
- 트레이 콜백을 메시지 큐 기반으로 바꿔, 콜백 안의 모달 대화상자가 창 프로시저를 재진입해도 안전하도록 정리했습니다.

### 참고
- 광고 판정 알고리즘 자체는 변경되지 않았습니다. 골든 fixture 10종의 기대값은 그대로이며, 적용 전용 경로가 기존 경로와 동일한 결과를 내는지 검증하는 회귀 테스트를 추가했습니다.

## 11.1.3 - 2026-09-09

시작프로그램에 등록되어 있어도 재부팅 후 실행되지 않던 경로를 수정한 패치입니다.

### 수정
- 로그온 직후 Explorer 트레이가 준비되기 전에 `NIM_ADD`가 실패하면 프로세스가 바로 종료되던 문제를 고쳤습니다. `Shell_TrayWnd`를 기다린 뒤 수 초에 걸쳐 재시도하고, 실패해도 메시지 루프를 유지한 채 `TaskbarCreated`/타이머로 아이콘을 다시 붙입니다.
- EXE를 다른 폴더로 옮기거나 이전 경로가 사라진 경우에도 Run 등록을 현재 실행 파일로 복구합니다. cargo 소스 실행은 기존 패키지 EXE 등록을 덮어쓰지 않습니다.
- Windows 설정/작업 관리자에서 시작 앱을 끈 상태(`StartupApproved`)를 감지하고, 시작프로그램 ON이면 다시 켭니다.
- Run 명령의 `\\?\` 경로 접두사를 제거해 부팅 시 실행이 막히지 않게 했습니다.
- 로컬 릴리스 빌드가 Visual Studio 2026 BuildTools(`vcvarsall.bat`)에서도 MSVC 링커를 찾도록 했습니다.

### 참고
- 이 버전을 한 번 실행하면 기존 시작프로그램 등록이 현재 EXE와 Windows 허용 상태에 맞춰집니다.

## 11.1.2 - 2026-09-06

감사(`PROJECT_AUDIT.md`)에서 확인된 실패 경로와 문서 불일치를 수정한 안정화 릴리스입니다.

### 수정
- Win32 자손 열거를 직계 자식 그래프로 정규화해 `popup_search_depth`가 실제 깊이를 따르게 했습니다.
- WM_CLOSE를 거부한 팝업이 숨김·표시를 반복하지 않도록, 숨긴 뒤에도 광고 시그널이 있으면 복원하지 않습니다.
- 복원 API가 한 번 실패해도 원본 스냅샷을 남겨 같은 프로세스에서 재시도합니다.
- rules/settings 필드 타입 오류가 시작 panic이나 전체 기본값 초기화를 일으키지 않습니다. 올바른 필드는 유지합니다.
- 트레이 생성 실패 시 워커를 복원·종료하고 프로세스가 mutex를 붙잡은 채 대기하지 않습니다.
- 업데이트 적용은 숨긴 창을 복원한 뒤에만 헬퍼를 실행합니다.
- 업데이트 확인은 한 번에 하나만 진행하고, 다운로드/헬퍼 경로는 실행마다 고유합니다.
- `--self-check` 보고서 쓰기 실패와 `--strict-self-check`를 종료 코드에 반영합니다. startup trace는 실제 트레이 결과를 기록합니다.

### 배포
- 권장 설치물은 `KakaoTalkLayoutAdBlocker_v11.zip`입니다. 앱 EXE와 `kakao-updater.exe`를 같은 폴더에 두어야 자동 업데이트가 됩니다.
- Windows CI와 release workflow가 헬퍼와 ZIP을 함께 올립니다.

### 기타
- Explorer `TaskbarCreated` 후 트레이 재등록, 시작프로그램 등록 누락 복구, 첫 실행 settings/rules 생성, 로그 회전, HTTP timeout.

## 11.1.1 - 2026-09-04

- 유휴 CPU 최적화 (liveness polling, zero-alloc 평가)
- 트레이 윈도우 생성 경로 수정
- 업데이트 artifact URL이 rust/legacy 저장소를 모두 허용
